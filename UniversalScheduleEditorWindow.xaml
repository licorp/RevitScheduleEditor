<Window x:Class="RevitScheduleEditor.ScheduleEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Universal Schedule Editor (Revit 2020-2026)" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Universal Schedule Editor" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text=" - Compatible with All Revit Versions" FontSize="12" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="Gray"/>
        </StackPanel>
        
        <!-- Schedule Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Schedule:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="1" Name="ScheduleComboBox" 
                      DisplayMemberPath="Name" 
                      SelectionChanged="ScheduleComboBox_SelectionChanged"/>
            <Button Grid.Column="2" Content="Refresh" Margin="10,0,0,0" Padding="10,5" Click="RefreshButton_Click"/>
        </Grid>
        
        <!-- Main Content Area -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="1" Margin="0,0,0,10">
            <ScrollViewer>
                <StackPanel Name="ContentPanel" Margin="10">
                    <TextBlock Text="Select a schedule to begin editing..." 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               FontSize="14" 
                               Foreground="Gray"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
        
        <!-- Status and Actions -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Name="StatusText" Text="Ready" VerticalAlignment="Center" Foreground="Green"/>
            
            <Button Grid.Column="1" Content="Apply Changes" Name="ApplyButton" 
                    Margin="0,0,10,0" Padding="15,5" 
                    Click="ApplyButton_Click"
                    IsEnabled="False"/>
            
            <Button Grid.Column="2" Content="Close" Name="CloseButton" 
                    Padding="15,5" 
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
