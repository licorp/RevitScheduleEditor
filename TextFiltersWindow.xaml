<Window x:Class="RevitScheduleEditor.TextFiltersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Text Filters" 
        Height="450" 
        Width="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <!-- Button Style -->
        <Style TargetType="Button" x:Key="DialogButtonStyle">
            <Setter Property="MinWidth" Value="75"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Padding" Value="8,2"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title and Instructions -->
        <StackPanel Grid.Row="0" Margin="10,10,10,5">
            <TextBlock Text="Text Filters" 
                       FontWeight="Bold" 
                       FontSize="14"/>
            <TextBlock Text="✅ Check items to SHOW them. Unchecked items will be HIDDEN." 
                       FontSize="11" 
                       Foreground="#666666" 
                       Margin="0,2,0,0"/>
            <TextBlock Text="💡 Tip: Use 'None' button, then check only what you want to see." 
                       FontSize="10" 
                       Foreground="#888888" 
                       FontStyle="Italic"
                       Margin="0,1,0,0"/>
        </StackPanel>
        
        <!-- Search Box -->
        <Border Grid.Row="1" 
                Margin="10,5,10,5" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1">
            <TextBox x:Name="SearchTextBox" 
                     Height="22"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     TextChanged="SearchTextBox_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Grid>
                                        <Border Background="White" 
                                                BorderThickness="0">
                                            <ScrollViewer x:Name="PART_ContentHost" 
                                                          Margin="4,2" 
                                                          VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Text="Search" 
                                                   Foreground="Gray" 
                                                   Margin="6,2" 
                                                   VerticalAlignment="Center"
                                                   IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Border>
        
        <!-- Filter Items List -->
        <Border Grid.Row="2" 
                Margin="10,5,10,5" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Select All Checkbox and Quick Actions -->
                <Border Grid.Row="0" 
                        Background="#F5F5F5" 
                        BorderBrush="#CCCCCC" 
                        BorderThickness="0,0,0,1" 
                        Padding="4,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <CheckBox x:Name="SelectAllCheckBox" 
                                  Grid.Column="0"
                                  Content="(Select All)" 
                                  FontWeight="Bold"
                                  Click="SelectAllCheckBox_Click"/>
                        
                        <Button Grid.Column="1" 
                                Content="Invert" 
                                Margin="5,0,5,0"
                                Padding="8,2"
                                FontSize="10"
                                Click="InvertSelectionButton_Click"
                                ToolTip="Invert current selection"/>
                        
                        <Button Grid.Column="2" 
                                Content="Clear All" 
                                Padding="8,2"
                                FontSize="10"
                                Click="SelectNoneButton_Click"
                                ToolTip="Uncheck all items (useful for selecting specific items)"/>
                    </Grid>
                </Border>
                
                <!-- Items ListBox -->
                <ListBox Grid.Row="1" 
                         x:Name="FilterItemsListBox"
                         ItemsSource="{Binding FilteredItems}"
                         SelectionMode="Multiple"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Value}" 
                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      Margin="4,2"
                                      ToolTip="{Binding Value}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E3F2FD"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>
        
        <!-- Status and Buttons -->
        <StackPanel Grid.Row="3" Margin="10,5,10,10">
            <!-- Status Info -->
            <TextBlock x:Name="StatusTextBlock" 
                       Text="{Binding StatusText}" 
                       FontSize="11" 
                       Foreground="#666666" 
                       Margin="0,0,0,5" 
                       HorizontalAlignment="Left"/>
            
            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button x:Name="OkButton" 
                        Content="OK" 
                        Click="OkButton_Click" 
                        IsDefault="True"
                        Style="{StaticResource DialogButtonStyle}"/>
                <Button x:Name="CancelButton" 
                        Content="Cancel" 
                        Click="CancelButton_Click" 
                        IsCancel="True"
                        Style="{StaticResource DialogButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
